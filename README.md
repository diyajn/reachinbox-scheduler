# ReachInbox Email Scheduler — Submission

Monorepo: `backend/` (Express + TS + BullMQ + Redis + Postgres + Ethereal),
`frontend/` (Next.js + Tailwind + TS).

See `backend/README.md` and `frontend/README.md` for setup + architecture
details. This file is the day-by-day build plan.

## Grant access

Add these GitHub users to the private repo (Settings → Collaborators):
`Mitrajit`, `Yadav036`
